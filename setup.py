from setuptools import setup, find_packages

setup(
    name='pedestrian_prediction',
    version='0.0.1',
    packages=find_packages()
    )

#setup(name='pedestrian_prediction', version='0.1.0', packages=['pedestrian_prediction'])

